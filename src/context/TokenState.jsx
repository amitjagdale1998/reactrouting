import React, { createContext, useState, useEffect } from "react";

const TokenContext = createContext();

const TokenState = (props) => {
  return <></>;
};

export default { TokenContext, TokenState };
